### йсм╪
```
/*йсм╪й╬юЩ1*/
create or replace algorithm = UNDEFINED view `v_user` as
select
   `t`.`user_id` as `ID_`,
   '1' as `REV_`,
   `t`.`user_name` as `FIRST_`,
   '' as `LAST_`,
   `t`.`user_mail` as `EMAIL_`,
   `t`.`user_pass` as `PWD_`,
   '' as `PICTURE_ID_`
from
   `t_sys_user` `t`;
```
