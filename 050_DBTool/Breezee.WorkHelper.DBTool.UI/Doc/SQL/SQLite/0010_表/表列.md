## 表列
### 查询所有表
```
SELECT NAME AS TABLE_NAME,CASE WHEN TYPE='view' THEN '1' ELSE '0' END AS TABLE_IS_VIEW
FROM main.SQLITE_MASTER 
WHERE TYPE IN ('table','view') 
    AND UPPER(NAME)= '#TABLE_NAME#'
```
### 查询表所有列
```
PRAGMA TABLE_INFO('#TABLE_NAME#')
```


