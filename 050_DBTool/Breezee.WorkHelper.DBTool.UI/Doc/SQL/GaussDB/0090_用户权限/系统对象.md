### 系统对象
```
--查询存储过程	
SELECT proname,	prosrc	
FROM pg_proc	
WHERE prokind = 'p'	
and proname = 'p_newcnccode_add';	
	
-- 查询名为'pkg_jf_pcoutil_new' 的Package	
SELECT pkgname,	
n.nspname,n.*	
FROM GS_PACKAGE p	
JOIN PG_NAMESPACE n ON p.pkgnamespace = n.oid	
WHERE pkgname = 'pkg_jf_pcoutil_new';	

```
