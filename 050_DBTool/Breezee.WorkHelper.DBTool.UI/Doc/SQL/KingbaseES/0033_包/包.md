## 包
在Oracle兼容模式下，金仓数据库（KingbaseES）支持创建和使用包。
### Oracle包迁移
```
1、创建一个空的同名新包：包名会自动转为小写。						
2、在【程序包】中右击刚刚创建的那个包 》生成SQL 》 DDL 》打开编辑器						
3、修改内容（分别从旧Oracle中复制包的声明和包体，然后粘贴），然后执行即可。						
4、一些常见的编译报错的修改点						
	Max(p.rowid)		>		Max(p.rowid::text)	

```