### 视图
KingbaseES中的视图会去掉注释，即执行前有注释，执行后注释会被去掉。
```
create or replace view v_test as
Select /*测试表查询：注释要写在select之后执行了才会显示，在select之前的注释会被忽略*/
      A.TEST_ID,
      A.TEST_CODE,
      A.TEST_NAME
 From T_TEST A /*测试表*/
;
```
