## Java-HTTP
### HttpEntity只能调用一次
在使用Apache HttpClient的EntityUtils.toString方法时出现的IOException问题（Attempted read from closed stream.）。
主要原因是该方法被错误地调用了多次，而HttpEntity只能被消费一次。即只能调用一次EntityUtils.toString(HttpEntity)。	
```

