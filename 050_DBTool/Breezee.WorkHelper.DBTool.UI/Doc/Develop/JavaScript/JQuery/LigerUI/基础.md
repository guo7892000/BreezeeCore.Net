## LigeUI-基础
### LigerUI控件 
```
//设置值
$("#item_id").ligerGetPopupEditManager().setValue(list[0].ITEM_ID,list[0].ITEM_ID);
$("#name_chn").ligerGetTextBoxManager().setValue(list[0].NAME_CHN);
$("#DEGREE_GENERAL").ligerCheckBox().setValue(false)
//取值
$("#item_id").ligerGetTextBoxManager().getValue()
$("#mfgCenterCode").ligerGetComboBoxManager().getValue()
$("#route_mfg_str").val()
$("#CNC_NEW_TYPE").ligerGetComboBoxManager().selected.id;
//设置下拉框选中值
$("#mfg_center_code").ligerGetComboBoxManager().selectValue(defaultMfgCenter);
//设置只读
$("#mfg_center_code").ligerGetTextBoxManager().setDisabled(true);
$("#mfgCenterCode").ligerGetComboBoxManager().setDisabled(true);
//清空组件值
$("#item_id").ligerGetPopupEditManager().clear();
$("#name_chn").val("");
//网格						
var currentRow = manager.getRow(rowindex);						//获取指定行数据
var currentRow=manager.getSelectedRows();						//获取所有选择行数据
var data = manager.getData();						//获取网格数据
var checkedRows = manager.getCheckedRows();						//获取所有选择行数据

$("#cncType").attr("value", cncMap[0].CNC_TYPE);			
$("#SERIES_RELA").attr("readonly", true);			
$('#opr_addnew').show()			
$('#opr_add').hide()			
liger.get("CNC_NEW_TYPE").setDisabled();			
$("#CNC_NEW_TEXT").val(rowData.CNC_CODE);			

//拼接所有选中行某列(逗号)	
var selectedRows = manager.getSelectedRows();	
let sItems = '';	
selectedRows.forEach(function(row) {	
	sItems += row['ITEM_ID']+",";  // 遍历选中的行数据
});	


```
### 绑定控件 
```
//绑定下拉框			
$("#MFG_CENTER_CODE_").ligerComboBox({			
    data:dt_mfgCenterCode,			
    valueField:'id',			
    textField:'text',			
    selectBoxWidth:178,			
    width:178,			
    cancelable:true,			
isShowCheckBox : false,			
isMultiSelect : false,			
    // split : ','			
// initValue: params.mfgCenterCode,			
});			
			
// 生产中心下拉框				
$("#mfg_center_code").ligerComboBox({				
    data:dt_mfgCenterCode,				
    valueField:'id',				
    textField:'text',				
    selectBoxWidth:178,				
    width:178,				
    cancelable:false,				
    onSelected: function (newIndex, item) {				
        $("#item_id").ligerGetPopupEditManager().clear();			
		$("#name_chn").val("");	
		$("#ver_num").val("");	
    }				
});				

//绑定按钮		
$("#cxBtn").ligerButton({		
	click : doSearchXm,	
	icon : "cx",	
	width : 70	
});		
		
//绑定日期		
$("#END_PFINISH_TIME").ligerDateEditor({		
    format : "yyyyMMdd",		
    selectBoxWidth : 178,		
    width : 250,		
    cancelable : true		
});		

//绑定下拉框				
$("#Erp_FlagConfig").ligerComboBox({				
    data: [{"id":"Y","text":"Y"},{"id":"N","text":"N"}],				
    valueField : 'id',				
    textField : 'text',				
    selectBoxWidth : 178,				
    width : 178				
});		

//绑定文本框				
"$(""#CSTR_CODE"").ligerTextBox({width: 168}).setDisabled();
$(""#CSTR_CODE"").ligerTextBox().setValue(“AA”);
$(""#CSTR_CODE"").ligerTextBox().setDisabled();"				
				
//设置下拉框默认值	
liger.get("CNC_NEW_TYPE").setValue("Q准");	
liger.get("CNC_NEW_TYPE").setText("艺准");	
liger.get("OBJ_TYPE").setValue(cncTypeRoute);	
有些除了设置value，还有设置text。有些只调value就可以了，很奇怪	
				
//复选框			
var btn_todefined={			
    id:"b_todefined",			
    text:"<input type='checkbox' name='todefined_yn' id='todefined_yn' value='1'>"+todefined,			
    click:searchCondition			
};			
liger.get("toolbarbtn").addItem(btn_todefined);			
$("#todefined_yn").ligerCheckBox();			
changeNoButtonStyle("b_todefined");			
				
//绑定弹出框：制造路线弹出页面		
$("#route_mfg_str").ligerPopupEdit({		
  cancelable : false,		
  onButtonClick : function (){		
      selectModel(true);		
  },		
  readonly : false,		
  width : 178		
});		
				
//Tab页签								
"$(""#framecenter"").ligerTab({
  height:$(window).height()-3,
  width:'670',
  onAfterSelectTabItem : function(pageid) {
   try {
    if (pageid == 'cxxz') {
     isFlagData = 'cxxz';
     if (isrefreshPage.page1) {
      if(isrefreshPage.refpage1){
      manager.options.newPage = 1;
      isrefreshPage.refpage1=false;
      }
      manager.loadData(true);
      isrefreshPage.page1 = false;
     }
    } else {
     isFlagData = 'xmxz';
     if (isrefreshPage.page2) {
      if(isrefreshPage.refpage2){
      manager_xm.options.newPage = 1;
      isrefreshPage.refpage2=false;
      }
      manager_xm.loadData(true);
      isrefreshPage.page2 = false;
     }
    }

   } catch (e) {
   }
  }
 });"								
								
//显示和隐藏Tab页面				
if(params.i_QueryPage=='xm'){				
    liger.get("framecenter").hideTabItem('cxxz');			
    liger.get("framecenter").selectTabItem('xmxz');			
}else{				
    liger.get("framecenter").selectTabItem('cxxz');			
}				
if(typeof(params.i_QueryPage)=='undefined'||params.i_QueryPage=='cx'){				
    liger.get("framecenter").hideTabItem('xmxz');   			
}				
								
//布局					
$("#mainlayout_xm").ligerLayout({					
	leftWidth : 270,				
	height : '100%',				
	onEndResize:function (parm,e){				
		if(isFlagData=='cxxz'){			
			isrefreshPage.page2=true;		
		}else if(isFlagData=='xmxz'){			
			isrefreshPage.page1=true;		
		}			
	},				
	onLeftToggle:function (isCollapse){				
		if(isFlagData=='cxxz'){			
			isrefreshPage.page2=true;		
		}else if(isFlagData=='xmxz'){			
			isrefreshPage.page1=true;		
		}			
	},				
	heightDiff : -1				
});					
								
// 网格：默认查询方法					
manager_xm = $("#maingrid_xm").ligerGrid({					
	columns : columns_xm,			
	url : contxtpath +tablemodelVersion + "/queryProject.action",			
	pageSizeOptions : [10,20,50,100,200],			
	root : "resultlist",			
	record : "rowcount",			
	parms :{		   	
		"filter": typeof(params.filter)=='undefined'?"":params.filter,			
		// 增加生产中心条件		
		"mfgCenterCode": $("#mfgCenterCode").ligerGetComboBoxManager().getValue()			
	},			
	delayLoad:true,			
	enabledSort : false,			
	usePager : true,			
	pageSize : 50,			
	dataAction : "server",			
	checkbox: true,			
	isSingleCheck: params.isCheckBox?false:true,			
	height : "100%",			
	heightDiff : -8,			
	onSuccess: f_onSuccess,			
	onError : showerror,			
});				
					
/*******************************************************************************						
 * 表格初始化						
 ******************************************************************************/						
$(function(){						
    if(!Empty($("#"+tablecode))){						
        $("#"+tablecode).ligerGrid({						
            columns:columns,						
            pageSizeOptions:[5,10,15,20,30,50],						
            url:contxtpath+tablemodel+'/getDataList.action',						
            // 增加生产中心值条件						
            //parms:[{name:'MFG_CENTER_CODE',value:sDefaultFactory}],						
            root:'resultlist', // 数据源字段名						
            record:'rowcount',						
            toolbar:{						
                id:'toolbarbtn'						
            },// 后面添加工具条						
            usePager:true,						
            pageSize:pagesize,						
            dataAction:'server',						
            rownumbers:true,						
            rownumbersColWidth:30,						
            checkbox : true, //增加选择框						
            width:'100%',						
            height:'100%',						
            heightDiff:-8, // 补高度差						
            onError:showerror						
        });						
        manager=$("#"+tablecode).ligerGetGridManager();						
    }						
})						
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								
								



```



