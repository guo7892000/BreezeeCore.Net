## Java-正则
### 使用ejs作为模板生成工程名为mongo_test的Express工程命令：								
	express --view=ejs mongo_test							
								
### 跨域：								
	当在页面上使用API的全地址时，只有后台服务（一般是SpringBoot微服务）支持跨域时，是可以直接访问的：
```	
	//本地SpringBootAPI测试							
	var apiServer = "http://localhost:8090"							
	var expressPre = "/myapi/apitest"							
	var apiPath01 ="/rest2/userlist"							
								
	function getSpringBootAPIFetch(){							
	let apiPath = apiServer + apiPath01//全路径API，测试通过							
	//apiPath = expressPre + apiPath01 //通过Express转发，访问失败							
	fetch(apiPath)							
	.then(data => {							
	console.info(data);							
	return data.json();							
	})							
	.then(res => {							
	let sList = res;							
	var app2 = document.getElementById("myApp2");							
	app2.innerHTML = "";							
	//title							
	let h2Title = document.createElement("h2");							
	h2Title.innerText = "自定义Rest服务API测试";							
	app2.appendChild(h2Title);							
								
	//data array							
	sList.forEach(x => {							
	let pEle = document.createElement("p");							
	pEle.innerText = x.name+":"+x.age;							
	app2.appendChild(pEle);							
	});							
								
	})							
	.catch(error => console.log('error is', error)); 							
	} 							
								
	当还要利用Express进行转发时，在使用express-http-proxy中间件转发时，出现跨域错误。但利用http-proxy-middleware中间件转发是正常的（其有跨域配置）。							
	HTML页面的JavaScript代码（客户端）：跟上面差不多，但API地址是相对地址，见红字部分							
	//本地SpringBootAPI测试							
	var apiServer = "http://localhost:8090"							
	var expressPre = "/myapi/apitest"							
	var apiPath01 ="/rest2/userlist"							
								
	function getSpringBootAPIFetch(){							
	let apiPath = apiServer + apiPath01//全路径API，测试通过							
	apiPath = expressPre + apiPath01 //通过Express转发，访问失败							
	fetch(apiPath)							
	.then(data => {							
	console.info(data);							
	return data.json();							
	})							
	.then(res => {							
	let sList = res;							
	var app2 = document.getElementById("myApp2");							
	app2.innerHTML = "";							
	//title							
	let h2Title = document.createElement("h2");							
	h2Title.innerText = "自定义Rest服务API测试";							
	app2.appendChild(h2Title);							
								
	//data array							
	sList.forEach(x => {							
	let pEle = document.createElement("p");							
	pEle.innerText = x.name+":"+x.age;							
	app2.appendChild(pEle);							
	});							
								
	})							
	.catch(error => console.log('error is', error)); 							
	} 							
	Express服务端：							
	使用http-proxy-middleware中间件（正常）：							
	var midProxy = require('http-proxy-middleware');							
	var express = require('express');							
	var app = express();							
								
	//方法1、利用http-proxy-middleware中间件：测试成功							
	var options = {							
	target: 'http://localhost:8090', // 目标服务器 host							
	changeOrigin: true, // 默认false，是否需要改变原始主机头为目标URL							
	//ws: true, // 是否代理websockets							
	pathRewrite: {							
	'^/myapi/apitest' : '', // 重写请求，比如我们源访问的是api/old-path，那么请求会被解析为/api/new-path							
	//'^/api/remove/path' : '/path' // 同上							
	}							
	};							
	var exampleProxy = midProxy(options);// 创建代理							
	app.get('/myapi/apitest/*', exampleProxy);							

使用express-http-proxy中间件（报错）：							
app.js							
var express = require('express');							
var app = express();							
var proxy = require('./routes/proxy');							
							
//方法2、利用express-http-proxy中间间							
app.get('/myapi/apitest/*',							
proxy.addClientTokenMiddleware,							
proxy.customProxyMiddlewareCORS('/myapi/apitest', 							
'http://localhost:8090','')							
); 							
							
proxy.js							
var expressProxy = require('express-http-proxy');							
							
var customProxyMiddlewareCORS = function(pathPrefix, endpoint, targetPath) {							
    console.log('custom endpoint: ' + endpoint);							
    return expressProxy(endpoint, {							
        https: true,							
        forwardPath: function (req) {							
            var path = req.url.replace(pathPrefix, targetPath || '');							
            console.log('proxying to:', path);							
            return path;							
        },							
        intercept: AddResponseHeaders,							
        decorateRequest: buildDecorator()							
    });							
};							
```	

### route
app.route()	
	app.route('/book')
	  .get(function (req, res) {
	    res.send('Get a random book')
	  })
	  .post(function (req, res) {
	    res.send('Add a book')
	  })
	  .put(function (req, res) {
	    res.send('Update the book')
	  })
	
### Router-level middleware	
	var app = express()
	var router = express.Router()
	
	// a middleware function with no mount path. This code is executed for every request to the router
	router.use(function (req, res, next) {
	  console.log('Time:', Date.now())
	  next()
	})
	
	// a middleware sub-stack shows request info for any type of HTTP request to the /user/:id path
	router.use('/user/:id', function (req, res, next) {
	  console.log('Request URL:', req.originalUrl)
	  next()
	}, function (req, res, next) {
	  console.log('Request Type:', req.method)
	  next()
	})
	
	// a middleware sub-stack that handles GET requests to the /user/:id path
	router.get('/user/:id', function (req, res, next) {
	  // if the user ID is 0, skip to the next router
	  if (req.params.id === '0') next('route')
	  // otherwise pass control to the next middleware function in this stack
	  else next()
	}, function (req, res, next) {
	  // render a regular page
	  res.render('regular')
	})
	
	// handler for the /user/:id path, which renders a special page
	router.get('/user/:id', function (req, res, next) {
	  console.log(req.params.id)
	  res.render('special')
	})
	
	// mount the router on the app
	app.use('/', router)
